export {};

let session = null;
let cache: string;

console.log(`Cache: ${cache}`);
//...//


// Typ null i undefined
session = null;
console.log(`Sesja jest usunięta: ${session}`);
console.log(`Cache: ${typeof cache}`);





